#include <stdio.h>

int multiply (int a, int b);

int main(){
int n1;
int n2;
printf("enter the numbers\n");
scanf("%d,%d", &n1,&n2);
int product = multiply(n1,n2);
printf("Product = %d", product);

return 0;
}

int multiply(int a, int b){
int result = a*b;
return result;
}
